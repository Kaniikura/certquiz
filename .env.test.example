# Test Database Configuration - Example Template
# Copy this file to .env.test and replace with your actual test database credentials

# Use a separate test database to avoid conflicts with development data
DATABASE_URL_TEST=postgresql://username:password@localhost:5432/certquiz_test

# Test-specific settings
NODE_ENV=test