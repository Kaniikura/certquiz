# Test Database Configuration
# Use a separate test database to avoid conflicts with development data
DATABASE_URL_TEST=postgresql://postgres:password@localhost:5432/certquiz_test

# Test-specific settings
NODE_ENV=test